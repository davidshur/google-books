import React from 'react';
import Jumbotron from 'react-bootstrap/Jumbotron';

const Header = () => (
    <Jumbotron className="mt-4">
        <h1>React Books</h1>
        <p>Use the Google Books API to search for and save books!</p>
    </Jumbotron>
);

export default Header;
